# Copy to access.env on the Pi after creating the Cloudflare Access application.
# These identifiers are configuration, not passwords, but access.env must not be public.
CF_ACCESS_TEAM_DOMAIN=your-team.cloudflareaccess.com
CF_ACCESS_AUD=cloudflare-access-application-audience-tag
ADMIN_EMAIL=your-google-account@example.com
